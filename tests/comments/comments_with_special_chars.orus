// Test comments with special characters and edge cases
x = 1 // Comment with "quotes" and 'apostrophes'
y = 2 /* Comment with symbols !@#$%^&*()_+-={}[]|\:";'<>?,./ */
z = 3 // Comment with numbers 123456789 and letters ABC

// Comment with escaped-like sequences \n \t \r \" \'
a = 4 /* Comment with /* false start but not nested */
b = 5 // Comment with // double slashes

// Comment with URL-like text http://example.com/path?param=value
// Comment with file paths /usr/local/bin and C:\Windows\System32

result = x + y + z + a + b
print(result)