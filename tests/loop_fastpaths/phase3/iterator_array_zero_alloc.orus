values = [2, 4, 6, 8]
mut count = 0
mut total = 0

for element in values:
    total = total + element
    count = count + 1

assert_eq("phase3 iterator_array_zero_alloc count", count, 4)
assert_eq("phase3 iterator_array_zero_alloc total", total, 20)
print("phase3-array-ok")
