iterable = range(8)
alias = iterable

mut seen = 0
mut total = 0

for value in alias:
    total = total + value
    seen = seen + 1

assert_eq("phase3 iterator_range_alias seen", seen, 8)
assert_eq("phase3 iterator_range_alias total", total, 28)
print("phase3-range-alias-ok")
