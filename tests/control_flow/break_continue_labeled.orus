'outer: for i in 0..3:
    'inner: for j in 0..3:
        if j == 1:
            continue 'inner
        if i == 2 and j == 0:
            break 'outer
        if j == 2:
            break 'inner
        print(i, j)

