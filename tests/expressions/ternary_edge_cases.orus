// Edge case tests for ternary operator
// Tests boundary conditions and corner cases

// Test 1: Ternary with nil values
x = nil
result = x == nil ? "empty" : "has value"
print(result)

// Test 2: Zero and negative numbers
zero = 0
sign = zero > 0 ? "positive" : zero < 0 ? "negative" : "zero"
print(sign)

// Test 3: Boolean true/false literals
flag = false
status = flag ? true : false
print(status)

// Test 4: Complex boolean expressions
a = 10
b = 5
c = 15
result2 = a > b and b < c ? "all conditions met" : "conditions failed"
print(result2)

// Test 5: Numeric type handling (same types)
int_val = 42
int_val2 = 30
result3 = int_val > int_val2 ? int_val : int_val2
print(result3)

// Test 6: String comparison (same types)
str_num = "123"
str_num2 = "456"
result4 = str_num == str_num2 ? "equal" : "not equal"
print(result4)

// Test 7: Maximum recursion depth test (moderate nesting)
depth = 3
nested = depth > 2 ? (depth > 1 ? (depth > 0 ? "deep" : "shallow") : "medium") : "surface"
print(nested)

// Test 8: Empty string handling
empty = ""
result5 = empty == "" ? "empty string" : "has content"
print(result5)
