// Basic ternary operator tests
// Tests the fundamental ternary syntax: condition ? true_expr : false_expr

// Test 1: Simple boolean condition
x = 5
result = x > 0 ? "positive" : "negative"
print(result)

// Test 2: String comparison condition  
name = "Alice"
greeting = name == "Alice" ? "Hello Alice!" : "Hello stranger!"
print(greeting)

// Test 3: Integer comparison conditions
age = 25
category = age >= 18 ? "adult" : "minor"
print(category)

// Test 4: Boolean variable as condition
isActive = true
status = isActive ? "active" : "inactive"
print(status)

// Test 5: Arithmetic expression as condition
score = 85
grade = score >= 90 ? "A" : score >= 80 ? "B" : "C"
print(grade)

// Test 6: Mixed type results
num = 10
result2 = num > 5 ? 42 : 0
print(result2)
