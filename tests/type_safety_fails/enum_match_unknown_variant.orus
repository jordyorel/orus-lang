enum Status:
    Ready
    Busy(reason: str)

fn handle(status: Status):
    match status:
        Status.Idle ->
            print("idle")
        Status.Busy(message) ->
            print("busy", message)
