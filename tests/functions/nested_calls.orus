// Nested function calls
fn double(x: i32) -> i32:
    x * 2

fn triple(x: i32) -> i32:
    x * 3

fn combine(a: i32, b: i32) -> i32:
    a + b

// Nested call: combine(double(5), triple(4))
result = combine(double(5), triple(4))
print(result)  // Should be 10 + 12 = 22