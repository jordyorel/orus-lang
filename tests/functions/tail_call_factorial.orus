// Test tail call optimization with factorial function
fn factorial(n: i32, acc: i32) -> i32:
    if n <= 1:
        acc
    else:
        factorial(n - 1, acc * n)

// Test with a reasonably large number
result = factorial(10, 1)
print("Factorial of 10 is:", result)