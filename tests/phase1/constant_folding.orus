// Phase 1 Constant Folding Tests
// Test compile-time evaluation of constant expressions

// Simple arithmetic folding
print(1 + 2)      // Should fold to 3
print(10 - 5)     // Should fold to 5
print(3 * 4)      // Should fold to 12
print(15 / 3)     // Should fold to 5
print(17 % 5)     // Should fold to 2

// Floating point folding
print(1.0 + 2.0)  // Should fold to 3.0
print(5.0 - 3.0)  // Should fold to 2.0
print(2.0 * 3.0)  // Should fold to 6.0
print(10.0 / 2.0) // Should fold to 5.0

// Type promotion in folding
print(1 + 2.0)    // Should fold to 3.0 (f64)
print(1.0 + 2)    // Should fold to 3.0 (f64)

// Complex constant expressions
print((1 + 2) * 3)    // Should fold to 9
print(10 / (2 + 3))   // Should fold to 2
print(2 * 3 + 4)      // Should fold to 10

// Unary operations
print(-42)        // Should fold to -42
print(-(1 + 2))   // Should fold to -3