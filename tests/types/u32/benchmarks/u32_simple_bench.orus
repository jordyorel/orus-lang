// Simple u32 arithmetic benchmark
// Using new Rust-like type inference
let mut sum: u32 = 0

let mut i: u32 = 0
let max_count: u32 = 1000
let increment: u32 = 1
while i < max_count:
    sum = sum + increment
    i = i + increment

print(sum)