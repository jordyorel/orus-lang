// u64 boundary value tests as required by AGENTS.md
// Testing boundary conditions: 0, 1, MAX_U64, overflow conditions
// Using new Rust-like type inference (no suffix needed with type annotation)

// Test zero and basic boundaries
zero: u64 = 0
one: u64 = 1
print(zero)
print(one)

// Test maximum u64 value (using suffix for very large literals)
max_val = 18446744073709551615u64  // 2^64 - 1  
print(max_val)

// Test medium values with type annotation
medium: u64 = 1000000
print(medium)

// Test arithmetic with boundary values (when compiler allows)
// These would test runtime u64 operations