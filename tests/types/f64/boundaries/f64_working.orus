// f64 edge case tests that work with current implementation
zero: f64 = 0.0
one: f64 = 1.0
neg_one: f64 = (0.0 - 1.0)
huge: f64 = 999999999.0
tiny: f64 = 0.000000001

print(zero)
print(one)
print(neg_one)
print(huge)
print(tiny)

// Test boundary arithmetic
sum: f64 = huge + tiny
diff: f64 = huge - tiny
product: f64 = tiny * 1000000000.0
print(sum)
print(diff)
print(product)