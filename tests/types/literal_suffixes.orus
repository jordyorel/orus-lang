// Type literal suffixes
a = 42        // i32 by default
b: u64 = 42   // u64 with explicit type
c: u32 = 42   // u32 with explicit type
d: u64 = 42   // u64 with explicit type
e: i64 = 42   // i64 with explicit type
f = 3.14      // f64 by default

print(a)
print(b)
print(c)
print(d)
print(e)
print(f)
