// Type literal suffixes (simplified without suffix support)
a = 42        // i32 by default
b: u64 = 42   // u64 with type annotation
c: u32 = 42   // u32 with type annotation  
d: u64 = 42   // u64 with type annotation
e: i64 = 42   // i64 with type annotation
f = 3.14      // f64 by default

print(a)
print(b)
print(c)
print(d)
print(e)
print(f)
