// Test type inference with same types (exact type matching)

// f64 arithmetic
a: f64 = 10.0
b: f64 = 2.5
result1 = a + b  // Should infer f64
print(result1)

// u32 arithmetic
x = 100u32  
y = 50u32
result2 = x + y  // Should infer u32
print(result2)

// i32 arithmetic (default type)
p = 25
q = 5
result3 = p * q  // Should infer i32
print(result3)

// i64 arithmetic
m = 42i64
n = 8i64
result4 = m + n  // Should infer i64
print(result4)