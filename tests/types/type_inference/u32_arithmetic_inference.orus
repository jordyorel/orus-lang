// Comprehensive u32 arithmetic type inference tests
a = 10u32
b = 5u32

// All basic operations should infer u32 type
addition = a + b
subtraction = a - b  
multiplication = a * b
division = a / b
modulo = a % b

print(addition)      // 15
print(subtraction)   // 5
print(multiplication) // 50
print(division)      // 2
print(modulo)        // 0

// Chained operations should also infer u32
complex = (a + b) * (a - b)
print(complex)

// With larger values
x = 4000000000u32
y = 2u32
result = x / y
print(result)