// Test that might reveal the issue
a: i64 = 1000
b: i64 = 999
c: i64 = 998

// Chain operation like in the benchmark
add_result: i64 = a + b + c
print("Add result:", add_result)

// Multiplication similar to the benchmark
mul_result: i64 = (a / 100) * (b / 100) * (c / 100)
print("Mul result:", mul_result)