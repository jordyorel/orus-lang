// Test simple recursion to verify register isolation works
fn countdown(n):
    print("Countdown:", n)
    if n > 0:
        countdown(n - 1)
    else:
        print("Done!")

print("Testing recursion (register isolation):")
countdown(3)