// Test inline expressions as function arguments
fn add(x, y) -> i32:
    return x + y

mut a = 2
mut b = 3

// These should work (they do)
print("a+1 =", a+1)  
print("b+1 =", b+1)

// This should also work but was failing before
result = add(a + 1, b + 1)
print("add(a+1, b+1) =", result)