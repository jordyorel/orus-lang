fn simple_test(n) -> i32:
    if n <= 0:
        return 0
    else:
        saved_var = n
        result = simple_test(n - 1) 
        print("n =", n, ", saved_var =", saved_var, ", result =", result)
        return saved_var + result

final = simple_test(3)
print("Final:", final)